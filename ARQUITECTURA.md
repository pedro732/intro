# 🏗️ ARQUITECTURA DEL SISTEMA DUAL

## 📊 Diagrama de Flujo

```
┌─────────────────────────────────────────────────────────────┐
│                    Usuario abre la página                    │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
        ┌────────────────────────────────────┐
        │  Detectar tamaño de pantalla       │
        │  window.innerWidth                  │
        └────────────┬───────────────────────┘
                     │
          ┌──────────┴──────────┐
          │                     │
          ▼                     ▼
    ┌─────────────┐       ┌──────────────┐
    │ ≤ 768px     │       │ > 768px      │
    │ (MÓVIL)     │       │ (DESKTOP)    │
    └──────┬──────┘       └───────┬──────┘
           │                      │
    ┌──────▼──────────┐   ┌──────▼──────────┐
    │ Carrusel        │   │ Carrusel        │
    │ • 4 imágenes    │   │ • 8 imágenes    │
    │ • 250px altura  │   │ • 400px altura  │
    │ • 5s timeout    │   │ • 8s timeout    │
    └────────┬────────┘   └───────┬────────┘
             │                    │
    ┌────────▼──────────┐   ┌────────▼──────────┐
    │ Indicadores       │   │ Indicadores       │
    │ • 3s timeout      │   │ • 5s timeout      │
    │ • Datos mínimos   │   │ • Datos completos │
    └────────┬──────────┘   └───────┬───────────┘
             │                      │
    ┌────────▼──────────┐   ┌────────▼──────────┐
    │ IA                │   │ IA                │
    │ • 10s timeout     │   │ • 20s timeout     │
    │ • Respuesta rápida│   │ • Mejor respuesta │
    └────────┬──────────┘   └───────┬───────────┘
             │                      │
             └──────────┬───────────┘
                        │
                        ▼
        ┌──────────────────────────────────┐
        │  Cargar Foto del Día             │
        │  Cargar Noticia del Día          │
        │  Mostrar Pie de Página           │
        └──────────────────────────────────┘
                        │
                        ▼
        ┌──────────────────────────────────┐
        │    Página completamente cargada  │
        │    ✅ Móvil: 1.5-2s              │
        │    ✅ Desktop: 2-3s              │
        └──────────────────────────────────┘
```

---

## 🔧 Lógica de Detección

```javascript
// En mounted() de cada componente
mounted() {
  // PASO 1: Detectar
  this.isMobile = window.innerWidth <= 768
  
  // PASO 2: Escuchar cambios
  window.addEventListener('resize', this.handleResize)
  
  // PASO 3: Cargar datos
  this.cargarDatos()
}

// ADAPTACIÓN AUTOMÁTICA
handleResize() {
  this.isMobile = window.innerWidth <= 768
}

// CLEANUP
beforeUnmount() {
  window.removeEventListener('resize', this.handleResize)
}
```

---

## 📱 Vista Móvil vs Desktop

### MÓVIL (≤768px)
```
┌──────────────────────────┐
│  NAVBAR                  │
├──────────────────────────┤
│  Intro (mostrado)        │
├──────────────────────────┤
│  Carrusel:               │
│  [IMG] [IMG] [IMG] [IMG] │  ← 4 imágenes
│  (250px alto)            │
├──────────────────────────┤
│  Tarjetas (pequeñas)     │
├──────────────────────────┤
│  IA:                     │
│  [Input para pregunta ]  │
├──────────────────────────┤
│  Indicadores (2 col)     │
│  [UF] [Dólar]            │
│  [Euro] [UTM]            │
├──────────────────────────┤
│  Foto del Día            │
├──────────────────────────┤
│  Noticia del Día         │
├──────────────────────────┤
│  FOOTER                  │
└──────────────────────────┘

⏱️  Tiempo carga: 1.5-2s
📊 Datos: ~40-50MB RAM
🖼️  Imágenes: 4
```

### DESKTOP (>768px)
```
┌────────────────────────────────────────────────┐
│  NAVBAR                                        │
├────────────────────────────────────────────────┤
│  Intro (mostrado)                              │
├────────────────────────────────────────────────┤
│  Carrusel:                                     │
│  [IMG] [IMG] [IMG] [IMG] [IMG] [IMG] [IMG] [IMG]│ ← 8 imágenes
│  (400px alto)                                  │
├────────────────────────────────────────────────┤
│  Tarjetas (normal)                             │
├────────────────────────────────────────────────┤
│  IA: [Input para pregunta     ]                │
├────────────────────────────────────────────────┤
│  Indicadores (4 col)                           │
│  [UF]     [Dólar]    [Euro]     [UTM]          │
├────────────────────────────────────────────────┤
│  Foto del Día          │      Noticia del Día  │
├────────────────────────────────────────────────┤
│  FOOTER                                        │
└────────────────────────────────────────────────┘

⏱️  Tiempo carga: 2-3s
📊 Datos: ~80-100MB RAM
🖼️  Imágenes: 8
```

---

## 🔄 Ciclo de Vida de Componente

```
┌─────────────────────────────────┐
│  Componente creado              │
│  (created) - EVITAR AQUÍ ❌     │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│  Componente montado en DOM      │
│  (mounted) - HACER AQUÍ ✅      │
│                                 │
│  1. Detectar isMobile           │
│  2. Agregar resize listener     │
│  3. Cargar datos con timeout    │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│  Componente renderizado         │
│  Usuario ve contenido           │
└────────────┬────────────────────┘
             │
    ┌────────┴────────┐
    │                 │
    ▼                 ▼
Resize    Usuario    beforeUnmount
actualiza interactúa
isMobile
             │
             ▼
┌─────────────────────────────────┐
│  Limpiar (beforeUnmount)        │
│  - Remover event listeners      │
│  - Limpiar timers               │
└─────────────────────────────────┘
```

---

## ⚙️ Parámetros Adaptativos

### Carrusel de Imágenes
```
┌─────────────┬──────────────┬──────────────┐
│ Parámetro   │ Móvil (≤768) │ Desktop (>768)│
├─────────────┼──────────────┼──────────────┤
│ Imágenes    │      4       │       8      │
│ Alto (px)   │     250      │      400     │
│ Timeout (s) │      5       │       8      │
│ Animación   │    0.3s      │     0.3s     │
│ Datos (MB)  │    ~20       │     ~40      │
└─────────────┴──────────────┴──────────────┘
```

### Indicadores Económicos
```
┌─────────────┬──────────────┬──────────────┐
│ Parámetro   │ Móvil (≤768) │ Desktop (>768)│
├─────────────┼──────────────┼──────────────┤
│ Timeout (s) │      3       │       5      │
│ Reintentos  │      0       │       1      │
│ Columnas    │      2       │       4      │
│ Datos (MB)  │    ~0.1      │     ~0.1     │
└─────────────┴──────────────┴──────────────┘
```

### Inteligencia Artificial
```
┌─────────────┬──────────────┬──────────────┐
│ Parámetro   │ Móvil (≤768) │ Desktop (>768)│
├─────────────┼──────────────┼──────────────┤
│ Timeout (s) │     10       │      20      │
│ Cache       │     No       │      Sí      │
│ Historial   │     No       │     Sí (5)   │
│ Respuesta   │   Rápida     │   Completa   │
└─────────────┴──────────────┴──────────────┘
```

---

## 🎯 Flujo de Optimización por Dispositivo

### MÓVIL
```
┌──────────────────────────────┐
│  REDUCCIÓN DE RECURSOS       │
├──────────────────────────────┤
│  • Menos imágenes (4)        │
│  • Imágenes más pequeñas     │
│  • Timeouts cortos           │
│  • Menos animaciones         │
│  • Menos datos en RAM        │
├──────────────────────────────┤
│  RESULTADO                   │
│  ✓ Carga rápido 1.5-2s      │
│  ✓ Usa 50% menos memoria    │
│  ✓ FPS estable 45-55        │
│  ✓ No se pega               │
└──────────────────────────────┘
```

### DESKTOP
```
┌──────────────────────────────┐
│  CALIDAD VISUAL              │
├──────────────────────────────┤
│  • Más imágenes (8)          │
│  • Imágenes grandes          │
│  • Timeouts normales         │
│  • Animaciones suaves        │
│  • Más datos disponibles     │
├──────────────────────────────┤
│  RESULTADO                   │
│  ✓ Mejor experiencia visual  │
│  ✓ Carga similar 2-3s        │
│  ✓ FPS estable 50-60        │
│  ✓ Mejor usabilidad         │
└──────────────────────────────┘
```

---

## 📡 Peticiones HTTP por Dispositivo

```
MÓVIL
└─ intro.vue
   ├─ pexels-images (4 img, 5s timeout)
   ├─ mindicador.cl/uf (3s timeout)
   ├─ mindicador.cl/dolar (3s timeout)
   ├─ mindicador.cl/euro (3s timeout)
   ├─ mindicador.cl/utm (3s timeout)
   ├─ mistral-chat (10s timeout)
   ├─ [Foto del día]
   └─ [Noticia del día]

DESKTOP
└─ intro.vue
   ├─ pexels-images (8 img, 8s timeout)
   ├─ mindicador.cl/uf (5s timeout)
   ├─ mindicador.cl/dolar (5s timeout)
   ├─ mindicador.cl/euro (5s timeout)
   ├─ mindicador.cl/utm (5s timeout)
   ├─ mistral-chat (20s timeout)
   ├─ [Foto del día]
   └─ [Noticia del día]
```

---

## ✨ Características Clave

```
┌─────────────────────────────────────┐
│  RESPONSIVO                         │
│  ✓ Se adapta a cualquier tamaño    │
│  ✓ Detecta cambios de ventana      │
│  ✓ Breakpoint: 768px               │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  TOLERANTE                          │
│  ✓ Maneja timeouts                 │
│  ✓ Maneja errores de red           │
│  ✓ Muestra mensajes útiles         │
│  ✓ No se queda esperando           │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  ESCALABLE                          │
│  ✓ Fácil de copiar patrón          │
│  ✓ Código reutilizable             │
│  ✓ Sin dependencias externas       │
│  ✓ Compatible con Vue 3            │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  EFICIENTE                          │
│  ✓ Reducción de memoria 50%        │
│  ✓ Mejor rendimiento en móvil      │
│  ✓ No afecta desktop               │
│  ✓ Cleanup automático              │
└─────────────────────────────────────┘
```

---

## 🚀 Que Esperar

### Primera Carga
```
┌──────────────────────────────────────┐
│ MÓVIL                  │ DESKTOP       │
│ 0-1.5s: HTML renderiza│ 0-2s: Igual    │
│ 1.5-2s: Contenido     │ 2-3s: Más data │
│ 2s: TODO LISTO ✓       │ 3s: TODO LISTO │
└──────────────────────────────────────┘
```

### Interacción
```
Carrusel → Smooth 45-55 FPS
Scroll → Suave, sin lag
Clicks → Respuestas inmediatas
Typing IA → Input responsivo
```

### Bajo Conexión Lenta (3G)
```
Móvil espera máximo 10 segundos
Desktop espera máximo 20 segundos
Si falla: muestra error claro
Si lento: muestra "Cargando..."
```

